CREATE TABLE film
  (
     id_film       INT auto_increment PRIMARY KEY,
     nama          VARCHAR(255) NULL,
     tanggal_rilis DATE NULL,
     harga         INT NULL,
     durasi        INT NULL
  ); 
